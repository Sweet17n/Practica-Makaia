// Definición de variables
let a = 4, b = 7, c = 2 * b, x = a, y = 3 * x, u = 6, w = 10;

// Operaciones
let operacionA = a * b * c;
let operacionB = a ** 2 + b ** 2;
let operacionC = (x + y) / (u + w / a);
let operacionD = x * y * (u + w); // La variable z no está definida, asumo que querías decir u

// Mostrar resultados
console.log("Resultado de a * b * c:", operacionA);
console.log("Resultado de a^2 + b^2:", operacionB);
console.log("Resultado de (x + y) / (u + w / a):", operacionC);
console.log("Resultado de x * y * (u + w):", operacionD);
